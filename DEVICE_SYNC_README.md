# 设备对时功能 (Device Sync)

## 功能概述

新增了第五个 Tab "设备对时"，允许两台 Apple 设备（iPhone、iPad、Mac）通过 Wi-Fi 或蓝牙直接比较各自的系统时钟差异，精度可达毫秒级。

## 核心特性

### 1. 自动发现与连接
- 使用 Apple MultipeerConnectivity 框架
- 自动发现附近的设备（Wi-Fi、蓝牙、同一局域网）
- 无需手动输入 IP 或配置网络

### 2. 角色分配
- **发起方 (Master)**：主动搜索并连接对方，发起测量
- **响应方 (Slave)**：广播自身存在，被动响应测量请求

### 3. 高精度测量算法
采用类似 NTP 的算法，但直接在两台设备间进行：

```
发起方 (Master)          响应方 (Slave)
    |                         |
    |---- t1 发送请求 ------->|
    |                         | t2 收到请求
    |<--- t3 发送响应 --------|
    | t4 收到响应             |
    |                         |
    计算: offset = t2 - (t1 + t4) / 2
         RTT = t4 - t1
```

### 4. 测量模式
- **精确测量**：执行多轮测量（5/10/20/50 轮可选），取中位数，消除网络抖动
- **持续监测**：每秒测量一次，实时显示时钟偏差变化

### 5. 结果展示
- 大号数字显示时间差（毫秒级）
- 往返延迟 (RTT) 显示
- 精度评估（极佳/良好/一般/较差）
- 测量详情：最小/最大/平均偏差、中位数
- 偏差分布图：可视化所有测量结果

## 精度说明

### 不同连接方式的精度
- **Wi-Fi 直连**：RTT 通常 <5ms，精度 ±1~3ms ⭐⭐⭐⭐⭐
- **同一局域网**：RTT 约 5~20ms，精度 ±3~10ms ⭐⭐⭐⭐
- **蓝牙连接**：RTT 约 20~50ms，精度 ±10~20ms ⭐⭐⭐
- **互联网连接**：不支持（MultipeerConnectivity 仅限本地通信）

### 影响精度的因素
1. 网络延迟的稳定性（RTT 越小越稳定）
2. 测量轮数（轮数越多，中位数越准确）
3. 设备负载（CPU 占用高时可能影响时间戳精度）

## 使用方法

### 步骤
1. 两台设备都打开 PrecisionClock，进入"设备对时" Tab
2. 一台设备点击"发起方"，另一台点击"响应方"
3. 发起方会搜索到附近的设备，点击"连接"
4. 连接成功后，点击"精确测量"或"持续监测"
5. 查看时间差结果

### 最佳实践
1. **先用 NTP 对时**：两台设备先分别连接同一个 NTP 服务器（如 time.apple.com），确保都校准到标准时间
2. **再用设备对时验证**：使用设备对时功能验证两台设备的时钟差异
3. **选择 Wi-Fi 直连**：尽量在同一房间，使用 Wi-Fi 直连获得最佳精度
4. **多轮测量**：使用 10 轮以上的精确测量，取中位数结果

## 与 NTP 对时的区别

| 特性 | NTP 对时 | 设备对时 |
|------|---------|---------|
| 时间源 | 远程 NTP 服务器 | 两台设备直接比较 |
| 精度 | ±10~50ms（取决于网络） | ±1~5ms（Wi-Fi 直连） |
| 距离限制 | 全球 | 本地（Wi-Fi/蓝牙范围） |
| 需要网络 | 是（互联网） | 否（本地通信即可） |
| 适用场景 | 校准到标准时间 | 比较两台设备的时钟差异 |

## 技术实现

### 文件结构
- `DeviceSyncManager.swift`：核心逻辑，管理 MultipeerConnectivity 会话和测量算法
- `DeviceSyncView.swift`：UI 界面，包含连接、测量、结果展示

### 关键类
- `DeviceSyncManager`：ObservableObject，管理设备发现、连接、测量
- `SyncMessage`：Codable 消息协议，定义请求/响应格式
- `DeviceSyncResult`：单次测量结果（偏差、RTT、时间戳）

### 消息协议
```swift
enum MessageType {
    case syncRequest   // 发起方 → 响应方：请求时间同步
    case syncResponse  // 响应方 → 发起方：响应时间同步
    case syncResult    // 发起方 → 响应方：发送最终结果
}
```

## 已知限制

1. **仅限本地通信**：MultipeerConnectivity 不支持互联网连接
2. **需要两台设备**：无法单设备测试
3. **iOS/macOS 专用**：依赖 Apple 专有框架
4. **时钟不可调整**：只能测量差异，无法自动同步时钟

## 未来改进方向

1. 添加历史记录功能，保存多次测量结果
2. 支持更多平台（通过自定义协议实现跨平台）
3. 添加时钟漂移监测（长期跟踪偏差变化）
4. 支持同时连接多台设备（网格对时）
5. 导出测量报告（CSV/PDF）
